# PydanticAI PR Plan

## Maintainer Fit Rationale
PydanticAI emphasizes typed interfaces and validation, which aligns well with `ghx` capability schemas and normalized envelopes.

## Likely Files and Paths
1. `docs/` tool integration guides.
2. `examples/` showing external API/tool orchestration.
3. Optional cookbook sections for agent task automation.

## Minimal Example
Provide a typed tool model around `ghx` task inputs and output envelope handling for a GitHub repo read workflow.

## PR Title Template
`docs(examples): typed ghx GitHub tool example for PydanticAI agents`

## PR Body Template
1. Alignment with typed tool philosophy.
2. Minimal sample with strict input/output shape.
3. Run instructions and expected response fields.
4. Placeholder claim policy.

## Benchmark Placeholder Note
Performance metrics remain placeholders until benchmark snapshot sign-off.

## Rejection Fallback
Publish the typed example in `ghx` docs with direct PydanticAI adaptation notes.
