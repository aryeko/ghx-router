---
"@ghx-dev/eval": minor
---

Implement three missing eval features: fixture seeding with `--seed-id`/`--dry-run`/`--config` flags, `eval analyze` command wiring session traces to profiler analyzers, and `eval report` command generating reports from JSONL results.
