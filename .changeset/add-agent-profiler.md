---
"@ghx-dev/agent-profiler": minor
---

Add `@ghx-dev/agent-profiler` package: a plugin-first framework for profiling AI agent session performance with built-in collectors, analyzers, statistical engine, and multi-page markdown reporter.
