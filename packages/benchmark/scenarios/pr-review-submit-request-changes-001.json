{
  "id": "pr-review-submit-request-changes-001",
  "name": "Batch A: submit pull request review requesting changes",
  "task": "pr.review.submit_request_changes",
  "input": {
    "owner": "OWNER_PLACEHOLDER",
    "name": "REPO_PLACEHOLDER",
    "prNumber": 0,
    "body": "Please address CI failures before merge."
  },
  "prompt_template": "Execute task {{task}} for repository {{fixture_repo}} using input {{input_json}}. Return only valid JSON matching this envelope: {\"ok\": boolean, \"data\": any, \"error\": object|null, \"meta\": object}.",
  "timeout_ms": 60000,
  "allowed_retries": 0,
  "fixture": {
    "repo": "aryeko/ghx-bench-fixtures",
    "bindings": {
      "input.owner": "repo.owner",
      "input.name": "repo.name",
      "input.prNumber": "resources.pr.number"
    }
  },
  "assertions": {
    "require_tool_calls": true,
    "min_tool_calls": 1,
    "required_fields": ["ok", "data", "error", "meta"],
    "required_data_fields": ["prNumber", "event", "submitted"],
    "required_meta_fields": ["route_used"],
    "expected_route_used": "cli",
    "expected_outcome": "success"
  },
  "tags": ["roadmap", "pr-exec", "pr", "review", "mutations"]
}
