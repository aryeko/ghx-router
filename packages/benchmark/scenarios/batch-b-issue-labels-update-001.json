{
  "id": "batch-b-issue-labels-update-001",
  "name": "Replace issue labels",
  "task": "issue.labels.update",
  "input": {
    "issueId": "ISSUE_ID_PLACEHOLDER",
    "labels": ["bug", "batch-b"]
  },
  "prompt_template": "Execute task {{task}} using input {{input_json}}. Return only valid JSON matching this envelope: {\"ok\": boolean, \"data\": any, \"error\": object|null, \"meta\": object}.",
  "timeout_ms": 60000,
  "allowed_retries": 0,
  "assertions": {
    "must_succeed": false,
    "require_tool_calls": true,
    "min_tool_calls": 1,
    "required_fields": ["ok", "data", "error", "meta"]
  },
  "tags": ["roadmap", "batch-b", "issue", "mutation"]
}
