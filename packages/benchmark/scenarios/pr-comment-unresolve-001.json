{
  "id": "pr-comment-unresolve-001",
  "name": "Unresolve pull request review thread",
  "task": "pr.comment.unresolve",
  "input": {
    "threadId": "THREAD_ID_PLACEHOLDER"
  },
  "prompt_template": "Execute task {{task}} using input {{input_json}}. Return only valid JSON matching this envelope: {\"ok\": boolean, \"data\": any, \"error\": object|null, \"meta\": object}.",
  "timeout_ms": 60000,
  "allowed_retries": 0,
  "assertions": {
    "require_tool_calls": true,
    "min_tool_calls": 1,
    "required_fields": [
      "ok",
      "error",
      "meta"
    ],
    "expected_outcome": "success"
  },
  "tags": [
    "pr",
    "mutations",
    "thread"
  ],
  "fixture": {
    "repo": "aryeko/ghx-bench-fixtures",
    "bindings": {
      "input.threadId": "resources.pr_thread.id"
    }
  }
}
